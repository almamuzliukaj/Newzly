// Export PostCSS configuration using CommonJS syntax (used in some environments like Node.js without ES modules)
module.exports = {
  plugins: {
    // Enable Tailwind CSS for utility-first styling
    tailwindcss: {},

    // Autoprefixer adds vendor prefixes to CSS rules for better browser support
    autoprefixer: {},
  },
};
